query SearchBySlug($slug: String!) {
  gameSearched(input: { slug: $slug }) {
    md5
    title
    description
    instruction
    company
    type
    subType
    company
    publishedAt
    categories
  }
}
